import React from 'react';

interface Props {}

export default function Header(props: Props) {
  return (
    <div
      className='site-layout-background'
      style={{ height: 60, fontWeight: 500, fontSize: 24, padding: '10px 20px' }}
    >
      TODO
    </div>
  );
}
